version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - ./fetch-secrets.sh
            - yarn install
        build:
          commands:
            - yarn run build
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: client
