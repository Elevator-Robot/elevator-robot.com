version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm install 18.0.0
            - nvm use 18.0.0
            - yarn install
        build:
          commands:
            - yarn run build
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: client
